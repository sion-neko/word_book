# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# 実装後の動作確認

コードの実装・修正を行ったら、必ず `android-emulator-verify` スキルを使ってAndroidエミュレーター上で動作確認すること。スクリーンショットを撮影し、UIが意図通りに表示・動作しているかを自分で確認してからユーザーに報告する。
